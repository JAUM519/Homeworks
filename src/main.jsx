import React from 'react'
import ReactDOM from 'react-dom/client'
import { ComponentApp } from './Challenge 4'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    < ComponentApp />
  </React.StrictMode>,
)
