import React from 'react'
import ReactDOM from 'react-dom/client'
import AtmQueue from './pages/AtmQueue'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AtmQueue />
  </React.StrictMode>,
)
